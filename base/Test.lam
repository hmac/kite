module Test

import Data.Bool (True, False, Bool, all)
import Data.Maybe (Just, Nothing)
import Data.Either (Either, Left, Right)
import Lam.Show (Show, show)
import Lam.Monoid (Monoid, concat)

e : Either Bool String
e = Left True

f : ?someHole
f = 4

main : String
main = "#{45 + 54} is a number"
