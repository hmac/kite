module Test

import Data.Bool (Bool(..), all)
import Data.Maybe (Maybe(..))
import Data.Either (Either(..))
import Lam.Show (Show(..))
import Lam.Monoid (Monoid(..), concat)
-- TODO: uncommenting this produces the error: Unknown variable Data.List.Nothing
-- import Data.List (subsequences)

e : Either Bool String
e = Left True

data Foo a = Foo { unFoo : a, tag : Int }

foo : Foo String
foo = Foo "isfoo" 1

-- one : Int
-- one = tag foo

-- isfoo : String
-- isfoo = unFoo foo

main : String
main = "#{45 + 54} is a number"
