module Data.Alternative

import Data.Applicative (Applicative)

type Alternative f = Alternative { applicative : Applicative f, empty : f a, choose : f a -> f a -> f a }

empty : Alternative f -> f a
empty (Alternative d) = d.empty

choose : Alternative f -> f a -> f a -> f a
choose (Alternative d) = d.choose
