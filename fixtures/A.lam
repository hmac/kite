module A

import B (defaultBar, Bar, MkBar, MkBoo)

a : Bar
a = MkBar 1

x : Int -> Int
x otherBar = otherBar

fac : Int -> Int
fac n = case n of
          0 -> 1
          n -> n * (fac (n - 1))

main : Bar
main = a
