module Compose

isZero : Int -> Bool {
  match {
    0 -> True,
    _ -> False
  }
}

not : Bool -> Bool {
  match {
    False -> True,
    True -> False
  }
}

notZero : Int -> Bool { not . isZero }

isZero2 : Int -> Bool { not . not . isZero }
