module T5

test : Int
test = append 1 2

type Semigroup a = Semigroup { append : a -> a -> a }

semigroupInt : Semigroup Int
semigroupInt = Semigroup appendInt

appendInt : Int -> Int -> Int
appendInt x y = x + y

type Foo = Foo Int
