module P4

# Return the elements of the list in reverse order.
reverse : [a] -> [a] {
  match { xs -> go xs [] }
}

go : [a] -> [a] -> [a] {
  match {
    [], acc        -> acc,
    x :: xs, acc -> go xs (x :: acc)
  }
}

imNotUsed : Bool { True }
